// Copyright (c) nami.ai

import SwiftUI

public struct CustomFinishingSetupView: View {
    public init() {
        
    }
    
    public var body: some View {
        VStack {
            Text("Should be replaced by some animation while finishing the setup of device")
                .padding(.horizontal)
        }
    }
}
